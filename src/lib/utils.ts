// Replace with the generated version once `shadcn` is initialized (adds clsx + tailwind-merge).
export function cn(...inputs: (string | undefined | null | false)[]) {
  return inputs.filter(Boolean).join(" ");
}
